# Shared env for sketchybar config and plugins

# AeroSpace CLI (used by workspace plugin)
AEROSPACE=aerospace

# Upcoming meetings ICS URL for SketchyBar calendar/meetings item.
# Set this to your calendar's iCal/ICS feed, for example:
# MEETINGS_ICAL_URL="https://calendar.google.com/calendar/ical/.../basic.ics"
MEETINGS_ICAL_URL="${MEETINGS_ICAL_URL:-}"
